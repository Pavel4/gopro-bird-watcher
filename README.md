# 🐦 GoPro Bird Watcher

Система автоматической записи видео с GoPro Hero 13 при обнаружении движения.
Идеально подходит для наблюдения за птицами на кормушке.

## ✨ Возможности

- 📡 Приём RTMP потока с GoPro по WiFi
- 🎯 Умная детекция движения с настраиваемыми порогами
- 🎬 Два режима записи:
  - **Автоматический** — при обнаружении движения
  - **Ручной** — по команде
- ⏪ Буфер: записывает N секунд **до** и **после** движения
- 🎯 **ROI (Region of Interest)** — запись только области кормушки:
  - Интерактивный выбор области мышкой
  - Детекция движения только в выбранной области
  - Crop видео для экономии места
- 🕐 Время по Москве в именах файлов
- 📊 Логирование для анализа

## 🏗 Архитектура

```
GoPro Hero 13 ──WiFi/RTMP──> nginx-rtmp ──> Motion Detector
     │                           │                │
     │                           └──> VLC/OBS     ├──> recordings/motion/
     │                                            └──> recordings/manual/
     └── SD карта (опционально)
```

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- GoPro Hero 13 (или другая с поддержкой RTMP)
- Одна WiFi сеть для GoPro и компьютера

### 1. Клонирование и запуск

```bash
git clone https://github.com/your-username/gopro-bird-watcher.git
cd gopro-bird-watcher
./scripts/start.sh
```

### 2. Настройка GoPro

В приложении **GoPro Quik** на телефоне:
1. Прямая трансляция → RTMP
2. RTMP URL: `rtmp://<IP-вашего-компьютера>/live`
3. Разрешение: 720p
4. Нажать "Начать трансляцию"

### 3. Включение записи при движении

```bash
./scripts/motion-on.sh
```

## 📋 Команды

### Система

| Команда | Описание |
|---------|----------|
| `./scripts/start.sh` | Запуск системы |
| `./scripts/stop.sh` | Остановка |
| `./scripts/status.sh` | Статус |
| `./scripts/select-roi.sh` | Выбрать область кормушки (ROI) |

### Автоматическая запись (при движении)

| Команда | Описание |
|---------|----------|
| `./scripts/motion-on.sh` | Включить |
| `./scripts/motion-off.sh` | Выключить |

→ Записи: `recordings/motion/bird_ГГГГ-ММ-ДД_ЧЧ-ММ-СС_XXmYYs.mp4`

### Ручная запись

| Команда | Описание |
|---------|----------|
| `./scripts/record-start.sh` | Начать |
| `./scripts/record-stop.sh` | Остановить |

→ Записи: `recordings/manual/manual_ГГГГ-ММ-ДД_ЧЧ-ММ-СС_XXmYYs.mp4`

## ⚙️ Настройки

Редактируйте `config.env`:

```env
# Буфер записи (секунды)
BUFFER_SECONDS=5           # Сколько секунд ДО движения записать
POST_MOTION_SECONDS=5      # Сколько секунд ПОСЛЕ движения записать

# Пороги для СТАРТА записи
MIN_CONTOUR_AREA=500       # Мин. площадь контура (пиксели)
MIN_MOTION_FRAMES=3        # Мин. кадров подряд для срабатывания
MOTION_AREA_PERCENT=3.0    # Мин. % площади кадра для старта записи

# Порог для ПРОДЛЕНИЯ записи
EXTEND_MOTION_PERCENT=0.2  # Мин. % движения чтобы продлить запись
                           # Меньше = дольше записывает при слабом движении

# Отладка
DEBUG_MOTION=false         # Показывать % движения в логах (для калибровки)
```

**Подстройка:**
- Много ложных срабатываний → увеличьте `MOTION_AREA_PERCENT`
- Пропускает птиц → уменьшите пороги
- Видео заканчивается до конца движения → уменьшите `EXTEND_MOTION_PERCENT`
- Для калибровки включите `DEBUG_MOTION=true` и смотрите логи

## 🎯 Настройка ROI (Region of Interest)

ROI позволяет записывать только область кормушки вместо полного кадра.
Это полезно когда камера снимает через окно или стекло — вы получаете:
- Меньше ложных срабатываний от фона (деревья, облака)
- Меньший размер файлов
- Более качественное детектирование птиц на кормушке

### Интерактивный выбор области

```bash
# Убедитесь что GoPro стримит
./scripts/select-roi.sh
```

Откроется окно с кадром из потока:
1. Нарисуйте прямоугольник мышкой вокруг кормушки
2. Нажмите **ENTER** для подтверждения
3. Координаты автоматически сохранятся в `config.env`
4. Перезапустите детектор: `docker-compose restart detector`

**Клавиши:**
- `R` — сбросить выбор и начать заново
- `Q` или `ESC` — отмена

### Ручная настройка

Если нет возможности запустить GUI, можно задать координаты вручную в `config.env`:

```env
# Включить ROI
ROI_ENABLED=true

# Координаты в пикселях (x, y - левый верхний угол)
ROI_X=200
ROI_Y=100
ROI_WIDTH=640
ROI_HEIGHT=400
```

### Определение координат из скриншота

1. Сделайте скриншот потока (VLC, OBS)
2. Откройте в редакторе (GIMP, Photoshop, Preview)
3. Найдите координаты левого верхнего угла кормушки (x, y)
4. Измерьте ширину и высоту области
5. Запишите в `config.env`

### Отключение ROI

Чтобы вернуться к записи полного кадра:

```env
ROI_ENABLED=false
```

## 📁 Структура проекта

```
gopro-bird-watcher/
├── config.env              # ⚙️ Настройки (редактируйте здесь)
├── docker-compose.yml      # Docker конфигурация
├── nginx.conf              # RTMP сервер
├── detector/
│   ├── Dockerfile
│   ├── motion_detector.py  # Детектор движения
│   ├── select_roi.py       # 🎯 Выбор области кормушки
│   └── requirements.txt
├── scripts/                # Скрипты управления
│   └── select-roi.sh       # 🎯 Запуск выбора ROI
├── recordings/
│   ├── motion/             # 🐦 Автозаписи
│   └── manual/             # 🎬 Ручные записи
├── logs/
└── control/
```

## 🔧 Разработка

Система использует единый контейнер `gopro-detector` для работы и разработки.

```bash
# Запуск контейнеров
./scripts/start-dev.sh   # Запустит оба контейнера и подключится к detector

# Или вручную:
docker-compose up -d
docker exec -it gopro-detector bash

# Внутри контейнера
python detector/motion_detector.py

# Управление через файл (внутри контейнера или на хосте)
echo MOTION_ON > control/command
echo RECORD_START > control/command
echo STATUS > control/command
```

**Контейнеры:**
- `gopro-rtmp-server` — RTMP сервер (готовый образ)
- `gopro-detector` — контейнер с Python, OpenCV и детектором движения

## 🐛 Troubleshooting

### GoPro не подключается
1. Проверьте что GoPro и компьютер в одной WiFi сети
2. Узнайте IP: `ipconfig getifaddr en0` (Mac) или `hostname -I` (Linux)
3. Проверьте что контейнеры запущены: `docker ps`

### Много ложных срабатываний
Увеличьте пороги в `config.env`:
```env
MIN_MOTION_FRAMES=5
MOTION_AREA_PERCENT=3.0
```

### Пропускает птиц
Уменьшите пороги:
```env
MIN_MOTION_FRAMES=2
MOTION_AREA_PERCENT=1.0
```

### Видео заканчивается до конца движения
Уменьшите порог продления:
```env
EXTEND_MOTION_PERCENT=0.1
DEBUG_MOTION=true  # Включите для диагностики
```

## 📷 Физическая установка

### Крепление камеры
1. Обезжирить поверхность спиртом
2. Приклеить Adhesive Mount (3M VHB)
3. Дать схватиться 2-3 часа
4. Закрепить GoPro

### Питание
- Power bank держать **внутри** помещения (литий не любит мороз)
- USB-C кабель 2-3м через приоткрытое окно

## 📜 Лицензия

MIT

## 🔮 Планы

- [x] ROI (Region of Interest) — запись только области кормушки
- [ ] ML-модель для распознавания видов птиц
- [ ] Telegram/Discord уведомления
- [ ] Веб-интерфейс для просмотра записей
- [ ] Статистика посещений кормушки
- [ ] Статус камеры: батарея, температура, параметры
- [ ] Получение текущих настроек камеры
